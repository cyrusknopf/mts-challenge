apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -o out
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: prism-server
  name: prism-server
spec:
  ports:
    - name: "8082"
      port: 8082
      targetPort: 8082
  selector:
    io.kompose.service: prism-server
